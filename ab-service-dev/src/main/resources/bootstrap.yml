spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: 47c0643a-0b4f-4a7b-a907-7e203ed677a9
        metadata:
          tag: 2.0
        group: ${spring.application.name}
  application:
    name: s2
server:
  port: 8083